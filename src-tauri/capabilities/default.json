{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissions for Menu Runner",
  "windows": ["main"],
  "permissions": ["core:default", "shell:allow-execute", "dialog:allow-message"]
}
